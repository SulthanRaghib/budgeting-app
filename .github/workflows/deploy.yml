name: Deploy to InfinityFree via FTP

on:
  push:
    branches:
      - master  # Ganti dengan nama branch utama Anda (master/main)

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: SETUP PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2' # Sesuaikan dengan versi PHP di InfinityFree Anda
          extensions: mbstring, bcmath, intl, gd, pdo_mysql

      - name: 📦 Install Composer Dependencies
        run: composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader

      - name: ⚡ Install NPM & Build Assets
        run: |
          npm install
          npm run build

      - name: 📂 Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5name: Deploy to InfinityFree via FTP

on:
  push:
    branches:
      - main  # Ganti dengan nama branch utama Anda (master/main)

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: SETUP PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2' # Sesuaikan dengan versi PHP di InfinityFree Anda
          extensions: mbstring, bcmath, intl, gd, pdo_mysql

      - name: 📦 Install Composer Dependencies
        run: composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader

      - name: ⚡ Install NPM & Build Assets
        run: |
          npm install
          npm run build

      - name: 📂 Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # Penting: Sesuaikan folder tujuan di hosting
          # Biasanya di InfinityFree folder publik adalah 'htdocs'
          server-dir: /htdocs/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/tests/**
            **/storage/*.key
            .env
            .env.example
            .editorconfig
            README.md
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # Penting: Sesuaikan folder tujuan di hosting
          # Biasanya di InfinityFree folder publik adalah 'htdocs'
          server-dir: /htdocs/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/tests/**
            **/storage/*.key
            .env
            .env.example
            .editorconfig
            README.md
